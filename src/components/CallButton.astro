---
import { config } from '../lib/config';
---
<a href={`tel:${config.tracking_phone}`}
   class="md:hidden fixed bottom-4 right-4 bg-amber-500 hover:bg-amber-400 transition-colors text-slate-900 font-bold px-5 py-3 rounded-full shadow-lg z-50">
  Call {config.tracking_phone_display}
</a>
